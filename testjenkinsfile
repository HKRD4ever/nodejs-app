pipeline {
    agent {
        docker {
            image 'node:14'
            args '-v /usr/local/bin:/usr/local/bin'
        }
    }
    stages {
        stage('Build') {
            steps {
                sh 'npm install'
            }
        }
    }
}
